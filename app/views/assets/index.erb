<div class="search-view">
<h3>Filter by:  <small><strong>Edit</strong></small> </h3>
<form class="form-inline" role="form" action="/assets" data-psg-form-method="PUT">
  <div class="form-group col-sm-6">
    <label for="study" class="col-sm-3 control-label">Study</label>
    <div class="col-sm-9">
      <input type="text" id="study" class="form-control" style="width: 100%" name="study" placeholder="Enter a study or abbreviation">
    </div>
  </div>
  <div class="form-group col-sm-6">
    <label for="study" class="col-sm-4 control-label">Workflow</label>
    <div class="col-sm-8">
<select id="workflow_id" name="workflow_id" style="width: 100%" class="form-control">
  <option value="None" default>Select a workflow</option>
<% presenter.each_workflow do |name, comment, id| %>
  <option value="<%= id %>" <%= (presenter.workflow==name) ? 'selected' : '' %> ><%= name %></option>
<% end %>
</select>
    </div>
  </div>

  <%= erb(:"assets/plates", :layout => false, :locals => {:presenter => presenter}) %>
  <%= erb(:"assets/tubes", :layout => false, :locals => {:presenter => presenter}) %>
  <%= erb(:"assets/samples", :layout => false, :locals => {:presenter => presenter}) %>

  <div class="form-group center-block">
      <button type="submit" class="btn btn-default">Update</button>
  </div>  
</form>


  </div>
  <div class="container" id="footer">
  </div>
</body>
</html>